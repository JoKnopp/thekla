EasySQL Deploy Professional allows the user to pack and compress database data and schema. It generates SQL scripts for user selected objects and pack them as .exe file. It can be used to deploy databases or database updates , to synchronize databases , to backup databases as SQL scripts. Command line interfaces are available to include the package in their installation kits or to automate the package generation. It supports MSSQL 2005 and 2000 .