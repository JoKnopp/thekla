AutoUpdate allows developers an easy way to deploy their updates on the host site and let users to add 'update over the web ' functionality to your software without a single line of code. Works with any development tool capable of generating apps which can host an ActiveX control. AutoUpdate uses configuration file it finds on the server of your choice and then determines if local files need to be replaced or not .